## Staging & Unstaging
<hr>

Staging files
```bash
~$ git add <filepath> [<filepath> <filepath>...]
~$ git add . (eq. git add -A)
```

Commit
```bash
~$ git commit -m ''
```

Undoing file changes
```bash
~$ git checkout <filepath>
```

Unstage a file
```bash
~$ git reset <filepath>
```
